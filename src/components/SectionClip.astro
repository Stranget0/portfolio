---
import type {Polymorphic, HTMLTag} from "astro/types";

type Props <T extends HTMLTag> = Polymorphic<{as:T}>

const { class: className,as:Tag="div", ...other } = Astro.props;
---

<Tag class:list={["clip-full", className]} {...other}>
	<slot />
</Tag>
