---
import AppearingLine from "@components/AppearingText/AppearingLine.astro";
import type {HTMLAttributes} 	from "astro/types"

interface Props extends HTMLAttributes<"div">{ title: string }

const { title, ...other } = Astro.props;
---
<!-- TODO: FIX TYPE -->
<AppearingLine
{...other as any}
	tag="h2"
	text={title}
	class="sticky z-12 top-4 text-subtitle-1 mb-4 mix-blend-difference mt-1px solid-text-shadow-sm solid-text-shadow-primary-200"
	group={1}
	lettersTransition
/>
